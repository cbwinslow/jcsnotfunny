{
  "name": "social-media-manager-mcp",
  "version": "1.0.0",
  "description": "MCP server for social media management operations",
  "main": "server.py",
  "scripts": {
    "start": "uvicorn server:app --host 0.0.0.0 --port 3000",
    "dev": "uvicorn server:app --host 0.0.0.0 --port 3000 --reload",
    "test": "pytest tests/"
  },
  "dependencies": {
    "fastapi": "^0.104.1",
    "uvicorn": "^0.24.0",
    "pydantic": "^2.5.0",
    "requests": "^2.31.0",
    "aiohttp": "^3.9.0",
    "python-multipart": "^0.0.6",
    "pillow": "^10.1.0",
    "moviepy": "^1.0.3",
    "pydub": "^0.25.1"
  },
  "devDependencies": {
    "pytest": "^7.4.0",
    "pytest-asyncio": "^0.21.0",
    "httpx": "^0.25.0"
  }
}