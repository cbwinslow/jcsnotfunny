# Project v2 Board — Example Items

- Episode pipeline: Improve end-to-end pipeline (ingest → transcribe → clip generation → upload)
- Clip automation: Use transcripts + silence detection + keywords for auto-clips
- Website improvements: structured data, better episode pages, speed optimizations
- Booking: build simple booking form + calendar + contract template
- Analytics: implement tracking and dashboards for clip performance
